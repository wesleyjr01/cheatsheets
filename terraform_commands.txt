# output terraform plan to a file (only what it matters)
terraform plan -no-color -var-file=development.tfvars -target=module.lambda_copy -lock=false | sed -n '/Terraform will perform the following actions:/,/to destroy./ p' > tfplan.txt
